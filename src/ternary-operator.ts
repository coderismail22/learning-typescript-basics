{
    // Ternary Operator
    //Structure: condition?expIfTrue:expIfFalse;
    
    let value = ""; // Works on all the falsy values [null, undefined, 0, false,"", NaN]

    const response = value ? "You can go" : "Stop !!!"

    console.log(response)


}